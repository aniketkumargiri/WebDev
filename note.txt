cd --> change directory  press tab (we will get all folder/file which is available in that particular folder)

cd .. --> cd (double dot) --> we will come back to the previous folder

Git command:-
git pull --> it will fetch branch/latest code from the master/main branch

git pull origin main
git add .
git commit -m "message"
git push
done!